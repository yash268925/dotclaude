---
name: github-writing
description: 後から第三者が読む日本語の文章を書くときの文体ルール。commit message、PR のタイトル / 本文、issue の投稿やコメント、リポジトリ内のドキュメント（README、設計メモ等）を書く / 編集するときに読む。git commit、gh pr create、gh issue create / comment を実行する前にも読む。Claude Code 上での応答や説明には適用しない。
---

# 成果物として残る文章の書き方

issue / PR / commit message / ドキュメントなど、会話の外に残り、後から第三者が読む日本語の文章に適用する。
Claude Code 上でのユーザーへの応答・説明・要約はこのルールの対象外であり、通常どおりでよい。

## ルール

- 文体は**である調**で統一する（ですます調と混在させない）。
- **宛先のない返信調にしない。** 依頼者との会話の続きをそのまま書かない。「ありがとうございます」「ご指摘のとおり」といった応答表現は、その場に相手がいない限り使わず、何をどう変えたかを事実として書く。
- 経緯ではなく結果を書く。会話の文脈を知らない読み手が、単体で読んで理解できる記述にする。

## 例

悪い例（commit message 本文）:

```
ご指摘のとおり、設定の読み込み順が逆になっていたので直しました。
ありがとうございます。
```

良い例:

```
設定の読み込み順を user → project の順に修正する。
project 側の値が user 側で上書きされる不具合を解消する。
```
